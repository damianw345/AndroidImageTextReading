<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="450dp">

        <ImageView
            android:id="@+id/imageView7"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/background" />

    </android.support.constraint.ConstraintLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:scrollbars="none"
        android:weightSum="10"
        tools:context="wasilewd.ee.pw.edu.pl.androidimagetextreading.MainActivity">

        <Button
            android:id="@+id/takePhotoButton"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_marginBottom="10dp"
            android:layout_marginTop="30dp"
            android:drawableTop="@drawable/camera"
            android:onClick="jumpToTakePhotoActivity"
            android:scrollbars="none"
            android:text="Take new photo"
            android:background="@android:drawable/dialog_holo_light_frame"
            android:textSize="20sp" />

        <Button
            android:id="@+id/LoadPhoto"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_marginBottom="15dp"
            android:layout_marginTop="10dp"
            android:drawableTop="@drawable/galery"
            android:nestedScrollingEnabled="false"
            android:onClick="jumpToLoadPhotoActivity"
            android:scrollbars="none"
            android:text="Load photo"
            android:background="@android:drawable/dialog_holo_light_frame"
            android:textSize="20sp" />

        <Button
            android:id="@+id/ShowResults"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_marginBottom="15dp"
            android:layout_marginTop="10dp"
            android:drawableTop="@drawable/result"
            android:nestedScrollingEnabled="false"
            android:onClick="jumpToShowResultsActivity"
            android:scrollbars="none"
            android:text="Show results"
            android:background="@android:drawable/dialog_holo_light_frame"
            android:textSize="20sp" />

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>

